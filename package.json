{
  "name": "@mountainpath9/big-rational",
  "url": "https://github.com/mountainpath9/big-rational",
  "version": "0.1.0",
  "main": "index.js",
  "types": "index.js.ts",
  "license": "AGPL-3.0-or-later",
  "private": false,
  "scripts": {
    "clean": "rm -rf dist",
    "build": "yarn clean && yarn tsc && yarn tsc-alias -p tsconfig.json && cp package.json README.md dist",
    "test": "yarn vitest run",
    "publish": "yarn && yarn build && cd dist && npm publish --access=public"
  },
  "devDependencies": {
    "@types/node": "^20.11.26",
    "tsc-alias": "^1.8.3",
    "typescript": "^5.4.2",
    "vite": "4.5.0",
    "vitest": "^1.3.1"
  },
  "dependencies": {}
}
